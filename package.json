{
  "name": "dxm-pulse",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api,sdk -c blue,green,yellow \"npm run dev -w apps/web\" \"npm run dev -w apps/api\" \"npm run dev -w packages/sdk\"",
    "dev:web": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "build": "npm run build -w packages/sdk && npm run build -w apps/web && npm run build -w apps/api",
    "lint": "npm run lint -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
